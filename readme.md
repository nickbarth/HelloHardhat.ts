# Hello World Hardhat Example

This project demonstrates a basic Hardhat usage in TypeScript.

Try running some of the following tasks:

```shell
npx hardhat help
npm run test
REPORT_GAS=true npx hardhat test
npm run node
npm run deploy
npm run greet
```
